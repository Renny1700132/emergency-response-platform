# 工程宪章（G3-02 规划初稿）

- 状态：PLANNED / REVIEW；M3 前仅可走变更流程修订，冻结后不得原位覆盖。
- 适用对象：人员、AI、代码、文档、OpenAPI、测试、配置与过程证据。
- 上位依据：P0 当前指令、真实《用户需求书》、受控 `spec.md`/RTM/控制文件与 `AGENTS.md`；冲突按 `governance/source_priority.md` 处理。

## 1 AI 与人工责任

1. AI 仅可在任务、受控输入、预期输出和验证方式均明确时辅助；不得代替人工裁决、签认、现场验收或风险接受。
2. 有产出任务须留存原始 Prompt、实际最终输出、修改、检查、失败与复核；不可取得的内容如实标注，不伪造。
3. AI 生成内容经主责人工核对来源、事实、数字、范围和可执行性后方可采纳；建议、Mock、草稿和候选不等于批准或通过。
4. 不向境外或第三方公共服务传输项目数据、凭据、个人敏感信息或甲方环境信息；提示词和日志须脱敏、受控留存。

## 2 范围、需求与决策

1. 每项计划、设计、实现和测试回指 FR、具体 AC、PE/KN 或受控 Issue；禁止镀金、静默裁剪和未批准承诺。
2. 不得改变冻结 FR、★、关键数字、裁决、甲乙责任或验收强度。歧义/缺口先建 Issue；范围变更按 Change/CCB 评估对 SRS、spec、OpenAPI、代码、测试、RTM、计划的影响。
3. 架构候选在 ADR 获得证据和所需人工决定前保持 Proposed，不得把评分、建议或原型结构写为 Accepted。

## 3 代码与文档

1. 代码仅在研发任务明确启动且设计/接口基线允许后编写；变更关联任务、设计 ID、FR/AC 和测试证据。禁止跨模块直接写入他模块数据。
2. 正式文档仅由唯一主责维护，必须遵循正式交付工作流、文档控制、修订记录、图表编号和渲染/结构检查；工作稿不得冒充正式件。
3. 代码、文档、配置、脚本和过程证据皆为配置项；不得删除/覆盖冻结基线或忽略过程证据。

## 4 测试与质量

1. 测试左移：设计完成即维护测试计划；实施前定义独立计算的预期、数据、环境、准入和准出，禁止从实现反推期望。
2. 验收均引用具体 AC；性能、容量、兼容、安全、外部连通性只报告真实环境、方法、原始测量和结论，未测写 `【待人工确认】`。
3. 缺陷按质量计划登记、分级、复测、关闭；门禁失败不得写通过，裁剪/豁免须书面批准与风险披露。

## 5 接口与数据

1. 先契约后实现。接口维护版本、鉴权、请求/响应、错误语义、`traceId`、超时、幂等和审计；OpenAPI 可解析且回指 FR/AC。
2. EXT-VIDEO、EXT-PUBLISH、EXT-INTRUSION、EXT-ACCESS、EXT-FIRE、EXT-IOT、EXT-MIDDLE、EXT-MESSAGE 保持独立语义；未证实字段/账号/环境明确 `【待人工确认】`。
3. 控制调用必须授权、二次确认、单次下发、回执对账；禁止盲目重放，失败审计并人工降级。Mock/单接口检查不替代 KN-064 端到端证据。
4. 数据仅在甲方内部受控环境部署/保存；按需求落实最小权限、脱敏、导出审计、逻辑删除、备份恢复和保存策略。

## 6 配置、变更、评审与冻结

1. 正式任务开始和结束遵守安全同步、非 force 提交/推送、最小暂存和日志回填；不处理来源不明修改，不重写共享历史。
2. 基线候选列明配置项、版本、依赖、检查与批准；冻结后发现问题以新版本/变更记录处理，不原位改写。
3. 主责自检后由指定复核人审查；意见以 Issue/Review 闭环。M3 仅在交付物、RTM、审计、开放 Issue 与准出一致时冻结。
4. 自 G4-01 起，`master` 不用于日常研发，只接收 `tasks.md` 指定唯一审核人完成 Review 并 Approve 后的 PR Merge。主责在功能分支完成实现、自检与本地 `npm run quality` 门禁并 push 后创建 PR；不要求 Jenkins、Gitee Go 或其他远程 CI。

## 7 提交前检查

每任务至少执行：来源/范围核对、AC/设计引用核对、适用静态或结构检查、复核记录核对、`git diff --check` 与最小暂存检查。G4 任务还须执行本地 `npm run quality`，并在 PR 中记录任务号、AI 参与范围、自检结果、审核结论和对应 commit。不能执行时如实记录原因、风险和解除条件。
