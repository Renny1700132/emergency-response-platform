# tasks.md —— “澜图”工具集原子任务清单（教学样例）

> 文档编号：LTPT-2026-G3-20（教学样例）　编制单位：通关实训第 3 组

**修订记录**

| 版本 | 日期 | 修订章节 | 修订说明 | 编制/修订人 |
|---|---|---|---|---|
| V1.0 | 2026-08-20 | 全部 | 原子任务清单（≤半天/带依赖/可独立验证）随 M3 冻结（D9） | 甲 |
| V1.1 | 2026-08-27 | 文本提示相关任务 | 按变更单 CR-001 更新阈值校验任务（D14） | 丙 |


> 版本 V3.0 · 快照时点 **D15 末（8/28 五 17:30，MVP 冻结点）**
> 基线：`spec.md` V3.0 / `plan.md` V3.0 / OpenAPI 契约快照 v3
> 纪律：**每个任务 ≤ 0.5 人日**——排不下就拆，不许把 1.5 天的活写成 0.5 天。每个任务必须有可独立验证的标准，且**验证标准一律引用 `spec.md` 的 AC 编号**，不许写"功能正常"。
> 全清单 36 项（T-01~T-36），其中 T-36 由变更单 CR-001 引入。本快照下 36 项全部完成，另有 4 项二期候选未开工（见 §5）。

**状态口径**：`待开始` / `进行中` / `待评审` / `已完成`（= 代码合并 + 测试通过 + 验证标准逐条勾验）。看板与本文件同源，站会后统一更新，**不允许两处状态不一致**。

---

## 1 工程基线（D7 · 8/18 二 · 8 项 / 4 人日）

对应 MS-1。这一天不写业务代码，只把"跑道"铺好——后面每一次提交都要从这条跑道上过。

| 编号 | 任务 | 负责人 | 估算 | 依赖 | 独立验证标准 | 状态 |
|---|---|---|---|---|---|---|
| T-01 | 仓库骨架与目录规范；pre-commit 挂 ruff（lint + format） | 甲 | 0.5 | — | 故意提交不合规范代码，pre-commit 拦截并给出规则编号 | 已完成 |
| T-02 | `AGENTS.md` / `constitution.md` 落位，验证 AI 工具会话自动加载 | 甲 | 0.5 | T-01 | 新开会话不粘贴规则，直接提问技术选型，AI 引用 R-01 白名单作答 | 已完成 |
| T-03 | CI 工作流：lint + 单测矩阵（ubuntu / macOS / Windows × py3.10 / 3.11 / 3.12） | 丁 | 0.5 | T-01 | 9 个组合全部跑通并出现在 PR 检查列表；MTN-03 前半 | 已完成 |
| T-04 | CI 门禁：覆盖率阈值 70%、`pip-audit` 高危阻断、`samgeo/` 上游目录只读校验 | 丁 | 0.5 | T-03 | 三条门禁各造一次违规提交，CI 均阻断合并（R-15 / SEC-02 / R-03） | 已完成 |
| T-05 | 容器镜像与一键启动脚本（CPU 模式） | 丙 | 0.5 | T-01 | 干净环境执行启动脚本，`GET /health` 返回 200；DT-01 前半 | 已完成 |
| T-06 | GPU 模式与设备探测回退 | 丙 | 0.5 | T-05 | 有 GPU 时解析到 cuda，无 GPU 时自动回退 CPU 且不报错；REL-01 | 已完成 |
| T-07 | 契约快照抓取脚本：`/openapi.json` → 归档为版本化快照 | 乙 | 0.5 | T-05 | 脚本产出快照文件，纳入版本管理，含全部 6 个端点 | 已完成 |
| T-08 | 契约测试骨架：快照比对，接口漂移必须转红 | 乙 | 0.5 | T-07 | 手工改一个响应字段名，契约测试立即失败；AC-FR-07.3-a | 已完成 |

---

## 2 迭代一（D9–D11 · 8/20 四 – 8/24 一 · 14 项 / 7 人日）

对应 MS-3：项目与会话、影像接入、模型管理三条基础链路。**D11 末中期评审锁范围。**

| 编号 | 任务 | 负责人 | 估算 | 依赖 | 独立验证标准 | 状态 |
|---|---|---|---|---|---|---|
| T-09 | 项目创建与 JSON 持久化（十字段结构 + 6 个默认分割参数） | 甲 | 0.5 | T-01 | AC-FR-01.1-a、AC-FR-01.1-b | 已完成 |
| T-10 | 项目信息查询与操作历史追加 | 甲 | 0.5 | T-09 | AC-FR-01.2-a、AC-FR-01.2-b | 已完成 |
| T-11 | 会话撤销 / 重做栈（含"撤销后新变更清空重做栈"语义） | 甲 | 0.5 | T-10 | AC-FR-01.3-a、AC-FR-01.3-b | 已完成 |
| T-12 | 会话持久化与跨进程恢复 | 甲 | 0.5 | T-11 | AC-FR-01.4-a | 已完成 |
| T-13 | 模型注册表查询：类型、变体清单、默认变体 | 乙 | 0.5 | T-01 | AC-FR-03.1-a、AC-FR-03.2-a | 已完成 |
| T-14 | 模型可用性检查与 extras 安装提示 | 乙 | 0.5 | T-13 | AC-FR-03.3-a、AC-FR-03.3-b；REL-01 | 已完成 |
| T-15 | CLI `project` / `model` 命令组（7 条子命令，全部支持 `--json`） | 乙 | 0.5 | T-12,T-14 | 逐条子命令 `--json` 输出可被 `json.loads` 解析；R-13 | 已完成 |
| T-16 | 栅格元数据核验 + 四类错误分支 | 丙 | 0.5 | T-01 | AC-FR-02.1-a、AC-FR-02.1-b | 已完成 |
| T-17 | 在线瓦片下载与影像合成 | 丙 | 0.5 | T-16 | AC-FR-02.2-a、AC-FR-02.2-b | 已完成 |
| T-18 | 坐标参考系重投影 | 丙 | 0.5 | T-16 | AC-FR-02.3-a | 已完成 |
| T-19 | 大影像分块切片（tile_size / overlap） | 丙 | 0.5 | T-16 | AC-FR-02.4-a、AC-FR-02.4-b | 已完成 |
| T-20 | 单测基线：project / session / model 三模块 | 丁 | 0.5 | T-12,T-14 | 三模块行覆盖率各 ≥70%，断言密度抽查通过；R-15 | 已完成 |
| T-21 | CLI `--json` 输出契约测试（子进程级） | 丁 | 0.5 | T-15 | 全部子命令 JSON 字段名稳定，改字段名即转红 | 已完成 |
| T-22 | 冻结影像集准备：5 景脱密影像 + 人工真值，记哈希封存 | 丁 | 0.5 | — | 哈希清单入库并在《需求确认书》附件登记；AT-01~03 的前置 | 已完成 |

---

## 3 迭代二（D12–D15 · 8/25 二 – 8/28 五 · 14 项 / 7 人日）

对应 MS-4 / MS-5 / MS-6：分割三模式、开放接口、矢量化与导出，收口到 MVP 冻结。

| 编号 | 任务 | 负责人 | 估算 | 依赖 | 独立验证标准 | 状态 |
|---|---|---|---|---|---|---|
| T-23 | COG 转换与底图清单查询 | 甲 | 0.5 | T-16 | AC-FR-02.5-a、AC-FR-02.5-b | 已完成 |
| T-24 | CLI `data` 命令组（5 条子命令） | 甲 | 0.5 | T-19,T-23 | 5 条子命令 `--json` 可解析；R-13 | 已完成 |
| T-25 | 掩膜栅格转矢量（简化容差 + 目标 CRS） | 甲 | 0.5 | T-16 | AC-FR-05.1-a、-b、-c | 已完成 |
| T-26 | REST 健康检查与模型端点（`/health`、`GET /models`、`DELETE /models`） | 乙 | 0.5 | T-05 | AC-FR-07.1-a、-b、-c | 已完成 |
| T-27 | REST 分割三端点接入（automatic / predict / text）与多格式响应 | 乙 | 0.5 | T-26,T-31 | AC-FR-07.2-a、AC-FR-07.2-c | 已完成 |
| T-28 | 模型缓存键设计：版本 + ID + 冻结构造参数，不同配置互不覆盖 | 乙 | 0.5 | T-26 | AC-FR-07.2-b；R-10 | 已完成 |
| T-29 | 全自动分割链路（6 参数可调） | 丙 | 0.5 | T-14,T-16 | AC-FR-04.1-a、AC-FR-04.1-b | 已完成 |
| T-30 | 点提示与框提示分割（含无提示 400 分支） | 丙 | 0.5 | T-29 | AC-FR-04.2-a、-b、-c；AC-FR-04.3-a、-b | 已完成 |
| T-31 | 文本提示分割（检测 + 分割两级，输出含置信度） | 丙 | 0.5 | T-29 | AC-FR-04.4-a、-b、-c | 已完成 |
| T-32 | 图斑过滤（面积阈值 / 属性条件）与矢量信息查询 | 丁 | 0.5 | T-25 | AC-FR-05.2-a、AC-FR-05.3-a、-b | 已完成 |
| T-33 | 六格式导出（GeoTIFF / COG / GeoPackage / Shapefile / GeoJSON / PNG） | 丁 | 0.5 | T-25 | AC-FR-06.1-a、-b、-c、-d；AC-FR-06.2-a | 已完成 |
| T-34 | CLI `vector` / `export` / `session` 命令组（7 条子命令） | 丁 | 0.5 | T-32,T-33 | 7 条子命令 `--json` 可解析；R-13 | 已完成 |
| T-35 | 覆盖率补齐至门禁并收口 CI（含 SCA 高危清零复核） | 丁 | 0.5 | T-34 | 核心模块覆盖率 ≥70%，9 组合 CI 全绿；MTN-03★、SEC-02 | 已完成 |
| T-36 | **【CR-001 引入】** 文本提示分割 box / text 阈值默认 0.30 → 0.24，同步 spec / SRS / 契约 / 用例 | 丙 | 0.5 | T-31 | 变更四步 commit 成链；AC-FR-04.4-a 在新阈值下重跑通过 | 已完成 |

---

## 4 变更登记

| 变更单 | 日期 | 内容 | 影响 | 处置 |
|---|---|---|---|---|
| CR-001 | D14（8/27） | 文本提示分割 box / text 置信度阈值默认 0.30 → 0.24，提升低对比度地物召回 | 新增 T-36；迭代二基线 +0.5 人日 | CCB 批准；`spec.md` V3.1 → SRS → `tasks.md` → 代码四步同步；燃尽基线显性上调，不悄悄消化 |

---

## 5 二期候选（本期不开工）

镀金禁令下的显式挂起项。挂在这里是为了**证明它们被想到过并被主动放弃**，不是遗漏。

| 候选 | 对应 FR | 挂起理由 |
|---|---|---|
| 会话跨进程恢复的并发写入保护 | FR-01.4 扩展 | 单人单会话场景未触发，本期不引入锁机制 |
| COG 批量转换模式 | FR-02.5 扩展 | 单文件转换已满足 MVP，批量属吞吐优化 |
| 图斑空间关系过滤（相交 / 包含） | FR-05.3 扩展 | 面积与属性过滤已覆盖去碎屑主诉求 |
| QGIS 插件离线分发包 | FR-07.3 扩展 | 本期以源码 + 安装说明交付，打包属发布工程 |

---

## 6 快照统计

| 指标 | 值 |
|---|---|
| 任务总数 | 36（含 CR-001 新增 1 项） |
| 已完成 | 36 |
| 计划工作量 | 18.0 人日（36 × 0.5） |
| 窗口容量 | 32 人日（D7 4 人日 + 迭代一 12 + 迭代二 16） |
| 排入率 | 56%（余量用于站会、评审、返工、文档同步） |
| 按时完成率 | 33/36 = 91.7%（T-30、T-31、T-35 各延后半天，均在迭代内追回） |

> 排入率 56% 控制在 `plan.md` 的 60% 上限以内——7 天窗口容量充裕，但 T-30/T-31/T-35 仍各延后半天：瓶颈不在总容量，而在依赖链关键节点（T-30/T-31 依赖 T-29）与验证返工。复盘结论：下一期关键路径任务提前半天启动，迭代二预留整天收口缓冲。**这条偏差写在这里而不是被抹平，是因为复盘的价值全在偏差里。**
