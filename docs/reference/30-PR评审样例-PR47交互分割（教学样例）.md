# PR #47：交互式分割会话服务（T-17）——PR 门禁全流程教学样例

> 文档编号：LTPT-2026-G4-30（教学样例）　编制单位：通关实训第 3 组

**修订记录**

| 版本 | 日期 | 修订章节 | 修订说明 | 编制/修订人 |
|---|---|---|---|---|
| V1.0 | 2026-08-26 | 全部 | PR #47（交互分割）评审记录归档：自查、评审意见、CI 结果、合并结论（D13） | 丙 |


> 本文件演示任务 4 的强制 PR 门禁：AI 生成 → 提交者自查 → 队友 Approve → CI 绿 → 合并。
> 关联：tasks.md T-17；SRS FR-03.1★/03.3/03.6、PE-01；设计 DLD 第 4 章（M-03）；契约 /segmentation/interactive。

## 0. PR 元信息

| 项 | 内容 |
|---|---|
| 标题 | feat(M-03): 交互式分割会话服务（点/框提示+多轮回退+确认写入） |
| 作者 | 同学丙（AI/算法方向） |
| 评审人 | 同学甲（后端 A，互评配对） |
| 关联任务 | T-17（验证标准：P95≤3s 五十次抽样；回退正确） |
| AI 参与声明 | 初稿由 AI 生成（提示词见文末附录 A），人工审查与重写记录见 §2 |
| 变更规模 | +312/-0 行（app/segmentation/interactive.py、tests/test_interactive.py） |

## 1. 自查清单（提交者必填，打勾需证据）

- [x] 需求映射：本 PR 覆盖 FR-03.1/03.3/03.6，无需求外功能（无镀金自查：逐函数指认 FR）
- [x] 契约一致：请求/响应结构与 OpenAPI v3 `/segmentation/interactive` 一致
- [x] 错误模型：全部异常走 BizError，HTTP 码合法（400/404/409），业务码在 1xxx/2xxx 段
- [x] 测试先行：本 PR 含 TC-F03-01~09 共 11 个用例，先红后绿（首跑失败截图见 CI #881）
- [x] 安全红线：scripts/security_redline.py 本地通过
- [x] 性能预算：elapsed_ms 埋点+预算断言（P95_BUDGET_MS=3000，推理预算分解见 DLD 4.2）
- [x] RTM 更新：FR-03.1/03.3/03.6 行回填代码文件与测试列（26 号文 v4 工作副本）

## 2. AI 初稿的人工审查记录（关键证据）

AI 初稿共 198 行，审查后保留/重写情况：

| # | AI 初稿问题 | 处置 | 教训 |
|---|---|---|---|
| 1 | 会话存全局可变单例，无并发隔离 | 重写为注入式 session_store | AI 默认写法不考虑并发 |
| 2 | 非法提示返回 500 而非 400 | 修正错误码段位 | 错误码必须人工核 |
| 3 | rollback 允许 steps=0 静默成功 | 增加 steps≥1 校验 + 用例 | 边界条件 AI 容易漏 |
| 4 | confirm 未校验空会话 | 增加空会话 409 + 用例 TC-F03-06-b | 状态机守卫要逐迁移核 |
| 5 | 编造了 `/segmentation/interactive/redo` 端点（契约中不存在） | 删除 | 镀金拦截：契约没有的端点不许实现 |

**采纳率**：约 74%（147/198 行保留），在健康区间（40%~75%）内。

## 3. 评审对话（节选，真实语气）

**同学甲（评审人）** 14:02
模型缓存未命中时的路径在哪？这个服务直接调 `_gw.predict`，如果 GPU 不可用触发 CPU 降级（REL-01），push_prompt 的行为是什么？现在看会把异常直接抛成 5000，降级路径没体现。

**同学丙（作者）** 14:15
好问题。当前 PR 范围只到会话服务层；降级在推理网关内实现（T-24），网关抛 4003，本服务透传。已补：docstring 注明降级联动，测试加一条网关抛 4003 时透传的用例（commit 9f3a2c1）。

**同学甲** 14:31
`rollback` 越界时你选择『清空到 0』而不是报错，依据是什么？

**同学丙** 14:33
DLD 4.3 异常流：回退越界按清空处理（交互体验裁决，避免用户卡在报错态）。已在代码注释引用 DLD 章节。

**同学甲** 15:05
其余 LGTM。Approve。合并前请确认 RTM 回填已提交。

## 4. CI 记录

| 闸 | 结果 | 证据 |
|---|---|---|
| Gate1 lint（ruff） | ✅ | CI #884 |
| Gate2 单测+覆盖率 | ✅ 11/11 通过；app/segmentation 92%（≥85% 门禁）；总体 76%（≥70%） | CI #884 coverage.xml |
| Gate3 引用核验 | ✅ | check_refs 输出 |
| Gate4 契约测试 | ✅ | contract_check 6 端点全过 |
| Gate5 安全扫描 | ✅ pip-audit 无高危；红线 R1~R4 过 | CI #884 |

> 注：首跑 CI #881 失败——测试先行纪律下先推了用例（红），实现随后（绿）。该记录用于证明测试护栏真实运转。

## 5. 合并与归档

- 合并方式：squash merge，commit 信息 `feat(M-03): interactive segmentation session (T-17, FR-03.1/03.3/03.6)`
- 合并后动作：tasks.md T-17 置 ✅；RTM v4 行更新归档；燃尽图数据点入库
- 复盘备注：AI 编造不存在端点（问题 5）已记入《翻车记录》FR-03

---

## 附录 A：本 PR 使用的提示词（入《Prompt 模式库》P-07）

```text
你是后端高级工程师。按以下契约与约束实现交互式分割会话服务（Python/FastAPI 服务层，不含路由）：
【契约】OpenAPI v3 /segmentation/interactive 片段（附）
【设计】DLD 4.2 类设计：SessionManager+InferenceGateway；异常流：提示无效/会话不存在/已确认/熔断透传
【硬约束】① 错误一律 BizError，HTTP 码只用 400/404/409；② 禁止实现契约外端点；
③ 1024² 裁剪常量 CROP_SIZE；④ 全程 perf_counter 计时返回 elapsed_ms；⑤ 不确定处列待裁决清单
【测试】先输出 pytest 用例清单（编号 TC-F03-xx），经我确认后再写实现
```

**模式要点**：契约先行+错误码段位锁死+『禁止契约外端点』反镀金条款+测试先行。
