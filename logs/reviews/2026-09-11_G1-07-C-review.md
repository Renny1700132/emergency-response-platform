# G1-07 风险登记册 C 复核记录

- Task ID：G1-07
- 主责：A
- 复核：C
- 日期：2026-09-11
- 复核对象：`docs/work/A_PM/risk_register_v1.md`、`docs/deliverables/04-风险登记册v1.docx`、`docs/work/A_PM/risk_register_v1_figures.pptx`
- 复核依据：`BASELINE-G1-V0.1`、`docs/work/G1-04_technical_risk_input.md`、`docs/work/A_PM/project_plan_v1.md`、`control/key_numbers.md`、`control/issues.md`
- 复核标准：按用户授权采用宽松准出标准；关键内容齐全、引用可追溯、评分无错、无明显冻结基线冲突即可通过。非阻断性格式细节和后续滚动复查事项不阻塞进度。

## 1 Preflight

`PFC-G1-07-C-01`：G1-07 的任务前置包含 G1-06，但 G1-06 当前仍处于 `REVIEW`；本次用户明确要求 C 先复核并完成 G1-07。该冲突不涉及真实项目需求、关键数字、范围或验收条件，按 `OVR-013` 执行：保留 13 项“待 G1-06 Review 后复核”的计划关联，作为后续非阻断滚动检查。

## 2 内容检查

| 检查项 | 结果 | 结论 |
| --- | ---: | --- |
| 风险条目 | RR-01—RR-16，共 16 项且编号连续 | 通过 |
| 必要字段 | 描述、类别、触发、后果、P、I、等级、策略、预防、应急、责任、状态、关联均存在 | 通过 |
| 风险覆盖 | 接口、数据语义、消息、视频、定位、地图、H5、性能、安全、部署、连续性、范围、进度、人员、AI、证据链 | 通过 |
| 概率影响计算 | 16/16 的风险值均等于 P×I，等级区间正确 | 通过 |
| WBS 引用 | 29 个引用均可在 G1-06 计划中定位 | 通过 |
| KN 引用 | 19 个引用均存在于 `key_numbers.md` | 通过 |
| Issue 引用 | 8 个引用均存在于 `issues.md`，且未把已关闭澄清误写为当前阻塞 | 通过 |
| 冻结口径 | 未发现范围、关键数字、甲乙责任或正式里程碑被弱化或擅自改变 | 通过 |

RR-01、RR-03、RR-04、RR-08、RR-10、RR-13 等高优先级风险具有预防、应急、责任和里程碑关联；重大风险 24 小时报告、M2 前视频/消息连通、H5 形态、等保边界、数据责任等口径与冻结基线一致。

## 3 Word 与图形检查

- Markdown 与 Word 均包含 RR-01—RR-16；Word 的风险评估表和详细关联表各有 16 条数据，编号全部一致。
- Word 含 3 个分节、3 张表和 1 个实际引用的内嵌图；无批注、无修订标记、无内部 Task ID。
- 实际引用的风险矩阵为更新后的 `image2.png`，16 个风险位置与 P/I 数据一致，文字清晰，无重叠或截断；旧 `image1.png` 仅为包内未引用资源，不影响正文显示。
- Word 中保留两处 `【待人工确认】`，分别用于尚未提供的文档编号和批准信息。该写法符合“不确定事实不得编造”的规则，不作为阻断项。
- 当前环境缺少 LibreOffice，捆绑 `render_docx.py` 无法启动；后台 Word 原生导出同样未成功。本次如实保留失败，不宣称完成原生逐页渲染。A 已完成分节、页边距、表格宽度、重复表头和图片尺寸的结构检查；结合本次内容、OOXML 结构及实际内嵌图检查，按用户授权的宽松标准不阻塞准出。后续使用可用 Word 环境打开时，可执行 `Ctrl+A`、`F9` 后另存 PDF 做一次非阻断分页复看。

## 4 结论

未发现影响第一关风险登记册使用的实质错误，也未发现需要登记阻断 Issue 的问题。G1-07 复核结论为 **ACCEPTED / DONE**。

后续仅保留两个非阻断动作：G1-06 完成后复查 13 项计划关联；在可用 Word 环境补做原生分页复看。两项均不得被描述为现场风险已经关闭或项目验收已经通过。
