# G4-02 A 独立审核记录

- 审核任务：G4-02｜正式后端、数据库及公共基础设施骨架
- 主责：B（严宇 / @yylinb）
- 唯一审核人：A（何思源 / @WhiteApricot）
- 候选分支：`origin/codex/g4-02-backend-foundation`
- 候选 HEAD：`4d110a8`
- 目标分支：`master`
- 审核结论：**FAIL**
- 批准/合并：**未批准、未合并**

## 1 范围与输入

审核严格限定在 G4-02 后端、数据库迁移和公共基础设施骨架，未进入 G4-05 的预案启动、事件、任务、回执或关闭材料等业务闭环。已核对 `BASELINE-G3-M3-R1.0`、数据库设计、详细设计、概要设计、冻结 OpenAPI、G4 工作流、`tasks.md`、Issue 与 `docs/reference/constitution.md`。后者明确为“澜图”教学样例，只能参考结构，不能作为本项目技术栈或业务事实来源。

## 2 阻断发现

### ISSUE-G4-02-001｜MAJOR｜公共表命名偏离冻结 DBD

`backend/migrations/001_foundation.up.sql` 创建 `audit_records`、`idempotency_records`、`outbox_messages`，但冻结 DBD 指定 `em_audit_log`、`em_idempotency_record`、`em_outbox_event`。这不是单纯代码风格差异：审计持久化 SQL 已直接依赖 `audit_records`，后续 G4-05 若按 DBD 接线将形成两套物理边界或额外迁移债。未见 CR/CCB、新 ADR 或基线变更批准，因此不允许合并。

### ISSUE-G4-02-002｜MAJOR｜后端核心代码未纳入 ≥70% 覆盖率门禁

`package.json` 的 `test:coverage` 仅包含 `scripts/g4/lib/*.mjs` 并只运行 `tests/g4/*.test.mjs`；`backend/src/*.mjs` 与 `tests/backend/*.test.mjs` 仅执行功能测试，不产生或检查后端覆盖率。候选门禁输出的 100% statements/lines/functions 与 90% branches 只代表 G4-01 护栏脚本，不能证明 G4-02 后端核心模块满足 KN-045。治理明确要求核心模块覆盖率 `≥70%`，因此当前不得批准。

## 3 通过项与观察项

- 正式 `backend/` 与 `prototype/` 隔离；未复用 localStorage、Mock 或 `prototypeStore`。
- 未修改冻结 OpenAPI、FR/AC、状态机或业务闭环；未提前实现 G4-05。
- 配置按环境变量注入，示例文件无凭据；秘密扫描与依赖审计通过。
- traceId、结构化错误、最小鉴权上下文、审计脱敏、health/readiness 均可运行。
- Outbox、幂等和审计仅建立公共接缝，没有实现业务派发；控制指令未被提前实现。
- OBSERVATION：G4-02 selfcheck 将真实迁移演练记为 `N/A`，本次 A 审核已在隔离 PostgreSQL 17 上补做成功。B 整改时应将可重复的实际迁移测试纳入自动化证据，而不是继续依赖人工环境。
- OBSERVATION：未定位到已创建的 Gitee PR；远端仅存在功能分支。整改后应创建单任务 PR，并指定 A 为唯一审核人。

## 4 实际执行测试

| 检查 | 结果 | 证据摘要 |
| --- | --- | --- |
| 干净安装 | PASS | 隔离工作树执行 `npm ci`，70 packages，0 vulnerabilities |
| backend 启动/health | PASS | 真实进程启动；`GET /healthz` 200 |
| readiness | PASS | 隔离 PostgreSQL 17；`GET /readyz` 200 |
| migration up/down/up | PASS | UP1 创建 3 公共表+迁移账本；DOWN 仅保留迁移账本；UP2 重新创建成功 |
| 最小鉴权 | PASS | 缺失身份 403；`emergency.read` 身份 200 |
| traceId / error response | PASS | 传入 traceId 在成功响应回显；404/403 返回稳定 code/message/traceId/retryable |
| 配置/秘密/依赖 | PASS | production 缺配置阻断；secret scan PASS；`npm audit` 0 vulnerabilities |
| 后端测试 | PASS | 4/4 |
| 统一本地门禁 | PASS（但覆盖率范围不合格） | G4 护栏 6/6；OpenAPI 0 error/14 个既有 warning；两份 selfcheck PASS |
| 覆盖率门禁 | FAIL | 输出未包含任何 `backend/src/*.mjs`，不能证明 KN-045 |
| `git diff --check` | PASS | 无空白错误 |

## 5 退回要求

由 B 修订并重新提交审核：

1. 使公共表与冻结 DBD 命名/语义一致；如确需不同物理命名，先走 Issue/CR/CCB 并提供兼容迁移与影响分析，A 不代替 B 作设计变更。
2. 将后端核心模块和后端测试纳入覆盖率采集与 `≥70%` 阻断门禁，保留真实输出。
3. 将 PostgreSQL `up → down → up` 纳入可重复测试或受控证据，更新 G4-02 selfcheck，不再写 `N/A`。
4. 从最新 `master` 安全吸收变化，重跑全部门禁并创建/更新单任务 PR，再请求 A 复审。

## 6 A 复审增量（候选 `d116ba5`）

- 分支关系：两个 G4-02 分支均从 `master@7e6c5b8` 分叉；A 分支仅含审核日志、审核记录和 Issue，无业务代码，不作为第二正式实现分支或 PR head。
- ISSUE-G4-02-001：B 已将公共表、索引、回滚脚本和审计 SQL 统一为冻结 DBD 的 `em_audit_log`、`em_idempotency_record`、`em_outbox_event`，PASS。
- ISSUE-G4-02-002：`npm run quality` 已纳入 `backend/src/*.mjs` 的四项 `≥70%` 覆盖率门禁；A 独立复跑得到 statements 94.77%、branches 93.93%、functions 93.75%、lines 94.77%，后端测试 6/6，PASS。
- 完整门禁：`npm ci` 0 vulnerabilities；G4 测试 11/11；OpenAPI 0 error/14 个既有 warning；秘密扫描 PASS；依赖漏洞 0；selfcheck PASS；`git diff --check` PASS。
- 迁移演练：可重复 `up → down → up` 命令和 A 先前 PostgreSQL 17 受控执行证据均已纳入候选；本次未伪造新的数据库执行。
- 复审结论：**PASS / 无新阻断项**。
- PR/Approve：尝试创建唯一 `codex/g4-02-backend-foundation → master` PR，但 Gitee OpenAPI 因本机凭据不是有效 access token 返回 401；故当前仍无 PR，尚不能形成真实 Gitee Approve 或 Merge 记录。
