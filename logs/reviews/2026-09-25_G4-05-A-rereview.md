# G4-05 A 独立复验记录

- 复验人：A（何思源 / @WhiteApricot）
- 主责人：B（严宇 / @yylinb）
- PR：!13（`codex/g4-05-core-event-backend` → `master`）
- 复验候选提交：`413565dba86a78c21126f0d42a13bc2091cd90e5`
- 复验时间：2026-09-25 18:36 +08:00
- 结论：PASS / ACCEPTED_FOR_APPROVAL；`ISSUE-G4-05-001—003` 全部关闭

## Issue 复验

### ISSUE-G4-05-001｜CLOSED / VERIFIED_BY_A

- 运行时核实请求已按冻结 OpenAPI 接受 `CONFIRMED`，内部持久状态映射不改变外部契约。
- 核实、启动响应、任务确认/反馈/完成和事件关闭均校验必填 `resourceVersion`，缺失请求被拒绝，版本冲突返回 409。
- 成功响应已具备 `code`、`message`、`traceId`、`timestamp`，事件创建返回冻结契约声明的 200。
- 契约正负例已覆盖上述行为；冻结 OpenAPI 未被修改。

### ISSUE-G4-05-002｜CLOSED / VERIFIED_BY_A

- 正式仓储具备事件、任务及事件关联任务的数据库加载能力；数据库模式下各命令在事务内恢复聚合及版本，不依赖进程内 `Map` 作为正式事实源。
- A 使用隔离 PostgreSQL 实例实际应用 `001_foundation`、`002_event_workflow` 迁移并执行集成测试；创建后重建工作流实例仍可继续核实、启动、确认、反馈、完成和关闭。
- PostgreSQL 集成测试 2/2 通过，证明服务实例重建后的事实恢复有效。

### ISSUE-G4-05-003｜CLOSED / VERIFIED_BY_A

- 数据库适配器已提供显式 `BEGIN/COMMIT/ROLLBACK`；业务事实、版本、Outbox 与审计在同一事务边界内写入。
- 响应启动先提交事件、任务及持久 Outbox，再执行提交后的消息派发；消息失败保留可恢复记录并进入人工处置，不在业务提交前同步形成外部事实。
- PostgreSQL 故障注入测试证明审计写入失败时业务事实与 Outbox 一并回滚；单元/集成测试同时覆盖提交后消息失败、事务顺序和恢复路径。

## 独立门禁

- 完整 `npm run quality`：PASS。
- 前端：27/27；覆盖率 statements 95.41%、branches 76.19%、functions 95.12%、lines 100%。
- G4 工程护栏：11/11；覆盖率 96.75% / 81.63% / 90% / 96.75%。
- 后端：14/14；覆盖率 statements/lines 87.38%、branches 85.76%、functions 80.95%。
- PostgreSQL 真实集成：2/2；迁移、跨实例恢复及事务回滚均通过。
- OpenAPI：0 error（14 个既有 warning）；秘密扫描 PASS；根目录与前端依赖漏洞均为 0；selfcheck PASS。

## 结论

三项原 MAJOR 均已按关闭条件完成整改并由 A 独立复验通过。PR !13 可由 A 执行平台 APPROVE，并通过该唯一 PR Merge 至 `master`。G4-05 的 DONE 仅在合并进入最新 `master` 后正式生效；G4-07 随后以该真实后端为输入继续准出检查。
