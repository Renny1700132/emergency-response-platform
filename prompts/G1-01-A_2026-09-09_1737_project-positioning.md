# G1-01-A 原始 Prompt

- captured_at: 2026-09-09 17:37 +08:00
- task_id: G1-01-A
- source_log: `logs/prompts/2026-09-09.md#log-g1-01-a-001`
- integrity: 以下内容为用户本次输入原文，未改写、润色或压缩。

````text
你现在执行 Day 1 · 0:20–1:10 中成员 A 的正式任务：

“项目价值和范围分析”。

本任务属于第一关“立项竞标”的前置分析工作，
不是正式《项目建议书》的直接定稿任务。

同时，本任务需要对 Git 工作流规则做一项小幅修订：

【今后每次正式任务开始之前，必须先从远程 master 同步最新内容，
尽量避免因为使用旧工作区开始工作而产生不必要的后续 merge。】

请实际执行，不要只给出建议。

\==================================================
0\. 本次任务身份
\==================================================

Task ID：

G1-01-A

Task Name：

项目价值与范围分析

执行角色：

A — 项目经理 PM / 总编 / 述标负责人

主要复核人：

C — 需求 / 质量 / AI 工程负责人

技术复核：

如涉及技术边界，由 B 提供复核意见。

本次任务的正式输出：

docs/work/A\_PM/project\_positioning.md

如果：

docs/work/A\_PM/

尚不存在，可以创建。

不要创建多个同义文件。

不要同时创建：

project\_scope.md
project\_value.md
business\_analysis.md
positioning\_analysis.md

等平行文件。

所有本次价值和范围分析统一进入：

project\_positioning.md


\==================================================
1\. 当前特殊情况：C 的事实基线尚未完成
\==================================================

正常工作流中，本任务应读取：

control/facts.md
control/key\_numbers.md
control/compliance\_matrix.md

作为项目事实基线。

但是当前：

C 暂时尚未完成并提交这些内容。

因此，本任务获得一次明确的临时授权：

【允许 A 直接读取 docs 中的真实《用户需求书-03-应急管理子系统》
开展本次项目价值和范围分析。】

这是临时 fallback，而不是改变项目治理体系。

必须遵守以下规则。


\--------------------------------------------------
1.1 本次唯一事实来源
\--------------------------------------------------

本任务关于真实项目事实，只允许使用：

《用户需求书-03-应急管理子系统》

如果 docs 中同时存在：

\- 指导书；
\- 任务书；
\- 教学案例；

它们可以用于：

\- 判断本阶段要分析到什么程度；
\- 参考文档结构；
\- 参考分析方法。

但不得用于填充本项目事实。


\--------------------------------------------------
1.2 不允许自行补出第二套 facts
\--------------------------------------------------

A 本次不得：

\- 接管 control/facts.md；
\- 接管 control/key\_numbers.md；
\- 自行建立与 C 平行的正式合规矩阵；
\- 将自己的分析结果声明为 BASELINE。

如果发现 control/facts.md 等目前只是空模板：

保持其主责归 C。

不要因为 C 尚未完成，
就在这些文件中大量填写内容。

否则会破坏唯一事实源制度。


\--------------------------------------------------
1.3 允许建立“工作级来源注释”
\--------------------------------------------------

在：

project\_positioning.md

中，每个重要结论应尽量标明对应的：

\- 用户需求书章节；
\- 条款；
\- FR；
\- PE；
\- 项目边界项；

但这些引用只是本工作稿自己的 source trace，
不是新的 control baseline。


\--------------------------------------------------
1.4 工作稿状态
\--------------------------------------------------

project\_positioning.md 顶部必须明确写：

Status: DRAFT\_PENDING\_BASELINE\_RECONCILIATION

并注明：

“本稿生成时 C 负责的 facts.md、key\_numbers.md 和 compliance\_matrix
尚未完成，因此当前直接依据原始《用户需求书》进行分析。
C 的正式事实基线进入仓库后，本稿必须执行一次 Baseline Reconciliation，
完成核对后方可作为《项目建议书》和《技术投标书》的稳定输入。”

不得标记为 FINAL。


\==================================================
2\. 任务开始前：先同步远程仓库
\==================================================

本次立即执行新的 Git 启动规则。

在对任何项目文件做修改之前：

先检查：

git status --short --branch
git remote -v

必须确认：

\- 当前位于 master；
\- origin 可用；
\- 工作区没有来源不明的未提交修改。


\--------------------------------------------------
2.1 如果工作区干净
\--------------------------------------------------

执行：

git fetch origin master

然后：

git pull --ff-only origin master

目的：

确保本次任务从最新远程 master 开始。


\--------------------------------------------------
2.2 如果本地 master 比远程领先
\--------------------------------------------------

如果只是存在已经提交但尚未 push 的本地 commit，
且没有发生分叉：

不要删除。

继续按安全 Git 流程判断。

不得 reset。


\--------------------------------------------------
2.3 如果本地与远程已经分叉
\--------------------------------------------------

本次任务开始阶段：

不要为了“开始工作”而立即制造新的 merge commit。

首先停止正式业务任务，
分析分叉来源。

如果可以明确判断：

某些本地提交本来就应当 push，
并且没有语义冲突，

按 governance/git\_workflow\.md 执行。

如果无法确认：

BLOCKED

向用户说明：

\- local HEAD；
\- remote HEAD；
\- 双方独有 commit；
\- 为什么不能自动处理。

不要猜。


\--------------------------------------------------
2.4 如果存在来源不明的未提交修改
\--------------------------------------------------

禁止：

git stash
git reset --hard
git restore .
git checkout .
git clean
git add -A

暂停并向用户确认。

不要覆盖其他成员正在进行的工作。


\==================================================
3\. 修改 Git 治理规则
\==================================================

当前仓库中已经存在 Git 治理规范。

优先查找：

governance/git\_workflow\.md

如果仓库实际已经改名为：

governance/git\_flow\.md

则修改实际存在的规范文件。

【不要为了名字差异再创建第二份 Git 规范。】

整个 Workspace 只能维护一份 canonical Git workflow。


\--------------------------------------------------
3.1 增加“Task Start Sync”规则
\--------------------------------------------------

加入明确规则：

\# Task Start Repository Sync

每项正式任务开始时，
在产生本 Task 的项目文件修改之前：

1\. 检查工作树；
2\. 确认 master；
3\. git fetch origin master；
4\. git pull --ff-only origin master；
5\. 确认本地已包含最新远程提交；
6\. 然后才进入 Prompt 留痕和任务执行。

推荐固定顺序：

REPOSITORY\_BOOTSTRAP
↓
REMOTE\_SYNC
↓
PROMPT\_LOGGED
↓
TASK\_EXECUTION
↓
VALIDATION
↓
OUTPUT\_LOGGED
↓
COMMIT
↓
FINAL\_REMOTE\_CHECK
↓
PUSH


\--------------------------------------------------
3.2 为什么默认使用 --ff-only
\--------------------------------------------------

规范中解释：

任务开始前同步的目标是：

“尽可能在工作开始之前吸收别人的修改”，

而不是：

“发现自己基于旧版本工作以后再制造 merge”。

因此默认：

git pull --ff-only origin master

如果无法 fast-forward，

说明当前历史已经产生分叉，
应该先识别原因，
而不是把 merge 当成例行步骤。


\--------------------------------------------------
3.3 任务结束时规则保持
\--------------------------------------------------

本次任务结束后仍然执行：

git fetch origin master

如果本 Task 执行过程中没有其他成员提交：

normal push。

如果远程在本任务期间产生新提交：

此时才允许按既有安全规则进行正常 merge。

若 Git 能无歧义自动合并：

可以合并。

若存在语义冲突：

必须交给用户。

严禁：

force
force-with-lease
reset --hard origin/master


\--------------------------------------------------
3.4 同步 AGENTS.md
\--------------------------------------------------

AGENTS.md 中 Git 简要规则补充：

Before every formal task:

1\. Verify clean/known working state.
2\. Fetch origin/master.
3\. Pull origin/master with --ff-only before creating task artifacts.
4\. If fast-forward is impossible, diagnose divergence before proceeding.
5\. Never start new work knowingly from a stale master.
6\. Never overwrite another member's work.

不要把整份 git\_workflow\.md 复制进 AGENTS，
只保留上述高频规则，
并引用 canonical governance 文件。


\==================================================
4\. Prompt 日志：按新规则执行本次任务
\==================================================

仓库同步完成后，
正式业务任务开始前，

立即执行：

governance/ai\_logging.md

的日志规则。

也就是说：

把用户本次 Prompt 完整原文逐字记录到：

logs/prompts/YYYY-MM-DD.md

并保存对应：

prompts/G1-01-A\_\<timestamp>\_project-positioning.md

本次状态：

PROMPT\_LOGGED

之后才能开始阅读和分析用户需求书。

不要只保存 Prompt 摘要。


\==================================================
5\. 本次项目分析的目标
\==================================================

本次不是写“漂亮的项目宣传文案”。

目标是给之后的：

《项目建议书》
《技术投标书》
项目计划
风险分析
述标

提供一份统一的：

【项目为什么值得做 + 项目到底做什么/不做什么】

的上游分析。

本轮重点回答六个问题：

1\. 这个项目为什么要建设？
2\. 它要解决什么业务问题？
3\. 它创造什么具体业务价值？
4\. 本项目实际建设范围是什么？
5\. 哪些内容明确不属于本项目？
6\. 项目成立需要依赖哪些既有系统、甲方资源和外部条件？

不要深入详细技术设计。

详细架构属于 B 的任务。


\==================================================
6\. 必须阅读的真实项目内容
\==================================================

完整阅读《用户需求书》，
但本任务重点提取以下内容：

1\. 项目背景；
2\. 建设目标；
3\. 建设原则；
4\. 建设内容；
5\. 项目边界；
6\. 与现有系统关系；
7\. 功能域总体结构；
8\. 数据接口关系；
9\. 实施和服务中的关键边界；
10\. 验收中体现出的核心业务价值。

必须特别关注需求书对项目的定位：

\- 实用实效；
\- 平战结合；
\- 融合联动；
\- 标准规范；
\- 安全可靠；
\- 开放可扩展。

不要把这些原则简单复制成六条口号，
而是分析：

“这些建设原则分别意味着项目要解决什么问题、产生什么价值。”


\==================================================
7\. project\_positioning.md 的完整结构
\==================================================

按以下结构生成。


\# 项目价值与范围分析

\## 0. 文档状态与依据

写明：

\- Task ID；
\- 主责 A；
\- 当前状态；
\- 当前事实来源；
\- C 的基线尚未完成；
\- 后续需要 Baseline Reconciliation。


\## 1. 项目定位

输出一个简明的一句话定位。

要求：

能回答：

“这个应急管理子系统本质上是干什么的？”

避免营销语言。

然后用 1–2 段展开：

\- 它在博物馆智能运营中心中的位置；
\- 日常管理与突发应急的关系；
\- 与既有安防系统的关系。


\## 2. 建设背景与业务问题

只从用户需求书支持的内容出发。

区分：

\### 2.1 原文明确指出的背景/目标

\### 2.2 从需求结构可以得出的业务问题分析

第二类必须明确标注：

【分析性结论】

不得写成：

“用户需求书明确指出……”

如果原文没有明确这样写。


重点分析可能包括：

\- 应急业务需要统一数字化管理；
\- 日常管理和应急处置需要形成平战结合；
\- 事件处置需要全过程闭环；
\- 现有视频、门禁、消防、物联网等系统需要联动而不是重复建设；
\- 指挥人员需要统一态势视图；
\- 移动人员需要形成现场反馈闭环；
\- 预案、演练、事件、资源和值班需要形成连续业务链。

以上只有在用户需求书能够支持时才写入。


\## 3. 项目核心价值

不要创造没有来源的 ROI 数字。

禁止写：

“效率提升 80%”
“成本下降 50%”
“响应速度提升若干倍”

除非用户需求书原文存在依据。

按价值维度分析：

\### 3.1 应急响应价值

例如：

从事件接报
→ 预案启动
→ 通知
→ 任务
→ 现场处置
→ 反馈
→ 关闭评估

形成闭环的意义。

\### 3.2 指挥协同价值

说明：

人员、视频、物资、事件等信息汇聚对统一指挥的价值。

\### 3.3 平战结合价值

说明：

预案、值班、物资、演练等日常管理如何为真正突发事件提供基础。

\### 3.4 系统融合价值

说明：

本项目重点是接口联动已有系统，
而不是重复建设前端安防设施。

\### 3.5 管理规范化与留痕价值

分析：

事件、任务、审批、反馈、演练、调查等全过程结构化记录的价值。

\### 3.6 安全与持续运行价值

仅依据用户需求书已有安全、可靠性要求分析。

不要自行增加安全等级或承诺。


\## 4. 建设范围（In Scope）

整理出清晰的范围树。

至少覆盖：

\### 4.1 应急预案管理
\### 4.2 应急指挥
\### 4.3 应急资源
\### 4.4 应急演练
\### 4.5 应急事件
\### 4.6 应急值班
\### 4.7 移动端
\### 4.8 应急信息视图 / 综合安防视图
\### 4.9 数据与系统接口
\### 4.10 配套文档、实施、培训与服务

每一项只描述：

“本项目需要建设什么能力”。

不要在这里设计具体框架或数据库。


\## 5. 明确范围外（Out of Scope）

这一节非常重要。

必须优先依据用户需求书“项目边界”的明确原文。

重点包括但不限于：

\- 视频监控系统等前端硬件及既有子系统自身建设/改造；
\- 应急物资等实体物资采购；
\- 服务器、机房、网络等基础硬件采购；
\- 与政府应急平台之间的专线联网工程。

必须区分：

“本项目不建设”

与：

“本项目需要接口对接”。

例如：

视频监控系统自身建设：
OUT OF SCOPE

视频监控系统接口联动：
IN SCOPE

不要混为一谈。


\## 6. 外部系统与项目依赖

建立表格：

\| 外部系统/资源 | 本项目关系 | 本项目负责 | 外部/甲方负责 | 风险/待澄清 |

至少检查：

\- 视频监控系统；
\- 信息发布系统；
\- 入侵报警；
\- 出入口控制；
\- 消防报警；
\- 物联网平台；
\- 统一业务运行中台；
\- 统一消息推送能力；
\- 甲方服务器/网络环境；
\- 馆内地图/楼层图等基础数据。

如果责任边界原文不够明确：

写：

【待人工确认】

不要替甲方补充职责。


\## 7. 关键利益相关者与使用角色

仅根据需求书中能确认的业务参与者整理。

例如：

\- 应急指挥人员；
\- 值班/安保人员；
\- 应急处置人员；
\- 物资管理员；
\- 系统管理员；
\- 其他能够从功能需求明确识别的角色。

表格：

\| 角色 | 核心诉求 | 主要使用能力 | 在应急闭环中的位置 |

不要创造用户画像年龄、技术水平等需求书不存在的信息。


\## 8. 业务闭环

用纯 Markdown 表述一个最核心业务闭环：

事件发现/上报
→ 核实
→ 预案启动
→ 通知和任务下发
→ 指挥调度
→ 移动处置
→ 反馈
→ 事件关闭/评估/调查

根据实际需求书核对后再使用。

同时说明：

哪些辅助域支撑这个闭环：

\- 预案；
\- 人员；
\- 物资；
\- 值班；
\- 演练；
\- 视频；
\- 地图；
\- 消息；
\- 外部接口。


\## 9. 主要项目约束

只列“价值和范围层面”需要知道的约束。

例如：

\- 必须复用既有系统；
\- 不重复建设硬件；
\- 要支持内网/隔离环境部署；
\- 标准协议要求；
\- Web + 移动端；
\- 安全和可靠性；
\- 开放可扩展。

性能数字如果确实需要引用：

必须直接引用用户需求书条款。

但本任务不要尝试建立自己的 key\_numbers 全集。


\## 10. 当前待确认问题

列出 A 在分析过程中发现但无法从需求书确定的问题。

格式：

Q-A-001
\- 问题：
\- 为什么影响价值/范围：
\- 当前依据：
\- 建议由谁确认：
\- 后续进入：
&#x20; \- C 的澄清清单
&#x20; \- 或 Baseline Review

这些问题不要由 A 自己拍板。


\## 11. 对后续文档的输入

说明本文件将向以下工作提供什么：

项目建议书：
\- 背景；
\- 价值；
\- 范围；
\- 边界。

技术投标书：
\- 需求理解；
\- 项目定位；
\- 建设范围；
\- 外部依赖。

项目计划：
\- 建设域；
\- 外部配合事项。

风险登记册：
\- 外部依赖；
\- 范围风险。

述标：
\- “为什么做”；
\- “做什么”；
\- “不做什么”。


\## 12. Baseline Reconciliation Checklist

建立待 C 提交事实基线后的检查清单：

[ ] 与 control/facts.md 逐项核对
[ ] 与 control/key\_numbers.md 数字核对
[ ] 与 compliance\_matrix 范围条款核对
[ ] 删除与正式事实基线冲突的表述
[ ] 将新的疑点进入 issues.md
[ ] C 完成符合性 Review
[ ] 状态由 DRAFT\_PENDING\_BASELINE\_RECONCILIATION 升级


\==================================================
8\. 分析质量要求
\==================================================

本文件必须做到：

1\. “价值”和“功能”分开。

不要把：

“支持应急演练管理”

直接当价值。

应该进一步说明：

它为什么对博物馆应急管理有价值。


2\. “范围内”和“范围外”必须成对检查。

尤其是外部系统：

既有系统自身建设 ≠ 接口对接。


3\. 原文与分析必须区分。

需求书明确说的：

写成事实。

从需求结构推导的：

标注为分析性结论。


4\. 不写虚假痛点数字。

没有来源：

不量化。


5\. 不替 B 做详细技术架构。

比如：

不要在本文件决定：

Kafka
RabbitMQ
Redis Cluster
PostgreSQL HA
Kubernetes

等具体实现。


6\. 不替 C 冻结事实。

本轮只是 A 的工作分析稿。


\==================================================
9\. 可使用 AI 的范围
\==================================================

AI 可以：

\- 提取用户需求书内容；
\- 聚类功能域；
\- 总结价值链；
\- 检查范围边界；
\- 构造表格；
\- 做反方检查；
\- 检查是否存在范围蔓延。

必须人工/主责确认：

\- 项目定位；
\- 价值判断；
\- 范围取舍；
\- 是否构成对外承诺；
\- 分析性结论是否合理。

完成初稿后，
请额外做一次自我对抗检查：

“假设你是 C（需求/质量负责人），
找出这份 project\_positioning.md 中所有：
1\. 无原文支持的事实；
2\. 未标记为分析性结论的推断；
3\. 范围扩张；
4\. 范围遗漏；
5\. 将案例事实误带入真实项目；
6\. 无来源数字。”

修复后再结束任务。


\==================================================
10\. 不允许使用的内容
\==================================================

本次禁止：

\- Web 搜索；
\- 外部行业数据；
\- 自行补充所谓博物馆行业平均数据；
\- 课件；
\- 案例中的项目事实；
\- 其他组项目内容；
\- AI 自行想象的客户痛点；
\- 没有需求依据的成本收益数字。

本次目标不是“丰富”，
而是“准确”。


\==================================================
11\. Review 状态
\==================================================

完成后：

project\_positioning.md

状态保持：

DRAFT\_PENDING\_BASELINE\_RECONCILIATION

同时在 tasks.md 中将：

G1-01-A

设置为：

REVIEW

而不是 DONE。

原因：

C 的正式事实基线尚未到位。

只有完成后续 Baseline Reconciliation
并由 C 复核后，

才允许标记：

DONE。


\==================================================
12\. 本次输出日志
\==================================================

任务成果完成并通过自检以后：

先生成本次准备返回给用户的完整执行结果，
写入同一 Prompt 日志：

ASSISTANT\_OUTPUT\_RAW

内容至少包含：

1\. 读取了哪个真实项目文件；
2\. 为什么本次使用临时 fallback；
3\. 创建/修改了哪些文件；
4\. project\_positioning 包含哪些部分；
5\. 发现多少个待确认项；
6\. 是否发现范围冲突；
7\. Git workflow 修改内容；
8\. 自检结果；
9\. 文件当前为什么仍是 DRAFT；
10\. 下一步需要 C 做什么。


\==================================================
13\. Commit 前检查
\==================================================

执行：

git status --short
git diff --check

确认：

\- 用户需求书未修改；
\- control/facts.md 未被 A 接管；
\- control/key\_numbers.md 未被 A 接管；
\- compliance\_matrix 未被 A 擅自冻结；
\- 无教学案例事实污染；
\- 未写无来源指标；
\- git workflow 只有一份 canonical 文件；
\- AGENTS 已同步 Task Start Pull 规则；
\- 本次 Prompt 原文已经记录；
\- ASSISTANT\_OUTPUT\_RAW 已记录。


\==================================================
14\. Commit
\==================================================

只 stage 本次任务相关文件。

禁止无检查直接：

git add -A

建议 commit：

docs(G1-01-A): analyze project value and scope

如果 Git 治理规则修改希望单独留痕，
也允许拆成两个 commit：

docs(governance): require pre-task remote sync

docs(G1-01-A): analyze project value and scope

优先保持历史清晰。


\==================================================
15\. Push 前再次同步
\==================================================

完成本地 commit 后：

git fetch origin master

检查远程是否在本任务期间发生变化。

如果没有：

git push origin master

如果远程有新增提交：

按照 governance/git\_workflow\.md：

\- 无歧义 → 正常 merge → 检查 → push；
\- 有语义冲突 → BLOCKED → 交用户裁决。

严禁 force push。


\==================================================
16\. 最终状态
\==================================================

成功后：

\- Git remote master 包含本 Task 提交；
\- G1-01-A = REVIEW；
\- project\_positioning.md =
&#x20; DRAFT\_PENDING\_BASELINE\_RECONCILIATION；
\- C 后续提交 facts/key\_numbers/compliance\_matrix 后，
&#x20; 必须执行新的 Baseline Reconciliation Task。


\==================================================
17\. 最终聊天输出
\==================================================

完整结果已经写入日志后，

正式聊天只返回本 Task 日志链接。

格式：

[查看 G1-01-A 完整任务日志]\(实际可访问日志链接)

如果当前环境无法生成可点击链接：

返回：

日志文件：
<实际路径>

并说明：

“当前环境无法生成可点击本地文件链接。”

不要再次在聊天中复制长篇执行报告。
````
