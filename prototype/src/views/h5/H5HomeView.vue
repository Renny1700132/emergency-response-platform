<script setup>import {computed} from 'vue';import {h5Modules} from '../../router/index.js';import {state} from '../../domain/prototypeStore.js';const pending=computed(()=>state.tasks.filter(x=>x.status!=='已完成').length)</script>
<template><section class="h5-page"><div class="h5-welcome"><small>您好，{{state.user.name}}</small><h2>移动应急</h2><p>快速处理现场事务</p></div><div class="h5-actions"><RouterLink v-for="x in h5Modules" :key="x.path" :to="`/h5/${x.path}`"><span>{{x.icon}}</span><strong>{{x.title}}</strong><small>{{x.description}}</small></RouterLink></div><div class="h5-card"><header><strong>待处理任务</strong><span>{{pending}}</span></header><p>请及时接收并反馈现场处置任务。</p></div></section></template>
